from setuptools import setup

setup(name='ALust_probability',
      version='0.1',
      description='Gaussian & Binomial distributions',
      packages=['ALust_probability'],
      author=['Arthur Lustman'],
      zip_safe=False)
