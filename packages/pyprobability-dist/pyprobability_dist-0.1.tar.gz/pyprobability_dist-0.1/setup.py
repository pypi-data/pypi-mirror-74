from setuptools import setup

setup(name='pyprobability_dist',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['pyprobability_dist'],
      zip_safe=False)
