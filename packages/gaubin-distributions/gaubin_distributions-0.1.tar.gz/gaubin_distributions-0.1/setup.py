from setuptools import setup

setup(name='gaubin_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['gaubin_distributions'],
      author='Shivansh Aggarwal',
      zip_safe=False)
