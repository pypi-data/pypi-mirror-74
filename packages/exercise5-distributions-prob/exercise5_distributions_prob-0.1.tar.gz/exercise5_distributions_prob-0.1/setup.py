from setuptools import setup

setup(name='exercise5_distributions_prob',
      version='0.1',
      description='Gaussian distributions',
      packages=['exercise5_distributions_prob'],
      zip_safe=False)
