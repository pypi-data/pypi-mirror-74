from setuptools import setup

setup(name='gitmodistributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['gitmodistributions'],
      zip_safe=False)
