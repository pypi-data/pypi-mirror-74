from setuptools import setup

setup(name='gauss-bino',
      version='1.1',
      description='Gaussian and Binomial distributions Utility Package from Majf Technologies.',
      packages=['gauss-bino'],
      zip_safe=False)
