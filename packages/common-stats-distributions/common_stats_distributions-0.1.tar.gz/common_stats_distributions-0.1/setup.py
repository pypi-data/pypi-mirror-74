from setuptools import setup

setup(name='common_stats_distributions',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['common_stats_distributions'], 
      author = 'Chaojie Wang',
      zip_safe=False)