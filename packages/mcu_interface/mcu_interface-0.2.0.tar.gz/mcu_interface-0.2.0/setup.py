#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['mcu_interface']

package_data = \
{'': ['*']}

install_requires = \
['pyudev>=0.22.0']

setup(name='mcu_interface',
      version='0.2.0',
      description='mcu_interface handles basic communication with MCUs',
      author='Jacob Alexander',
      author_email='haata@kiibohd.com',
      url='http://kiibohd.com',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      python_requires='>=3.7',
     )
