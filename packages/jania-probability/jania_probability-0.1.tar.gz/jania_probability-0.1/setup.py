from setuptools import setup

setup(name='jania_probability',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['jania_probability'],
      author = 'Jania Mulanovich', 
      zip_safe=False)
