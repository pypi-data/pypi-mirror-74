[tool.poetry]
name = "poetry-packaging-poc"
version = "0.3.0"
description = ""
authors = ["Marcos Damián Mesmer y Rosset <marcosdmyr@gmail.com>"]

[tool.poetry.dependencies]
python = ">=3.8"
click = "^7.1.2"

[tool.poetry.dev-dependencies]

[tool.poetry.scripts]
poetry-packaging-poc = "poetry_packaging_poc.console:main"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
