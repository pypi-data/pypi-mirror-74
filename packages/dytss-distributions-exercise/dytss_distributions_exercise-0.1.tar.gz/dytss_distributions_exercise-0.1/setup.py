from setuptools import setup

setup(name='dytss_distributions_exercise',
      version='0.1',
      description='Gaussian and Binomial Distributions',
      packages=['dytss_distributions_exercise'],
      zip_safe=False)
