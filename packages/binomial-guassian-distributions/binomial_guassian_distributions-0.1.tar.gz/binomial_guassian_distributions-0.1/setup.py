from setuptools import setup

setup(name='binomial_guassian_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['binomial_guassian_distributions'],
      zip_safe=False)
