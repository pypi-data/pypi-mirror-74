from setuptools import setup

setup(name='distributionsfeena',
      version='0.2',
      description='Gaussian distributions',
      packages=['distributions'],
      author = ' Rafeena ',
      zip_safe=False)
