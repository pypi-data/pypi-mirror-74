from setuptools import setup

setup(name='binomial_package_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['binomial_package_distributions'],
      author='Abhishek',
      zip_safe=False)
