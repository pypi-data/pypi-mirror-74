from setuptools import setup

setup(name='mengyu_distributions',
      version='1.1',
      description='All kinds of distributions',
      packages=['mengyu_distributions'],
      zip_safe=False)# cannot be run directly when ziped
