from setuptools import setup

setup(name='testing_package_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['testing_package_distributions'],
      zip_safe=False)
