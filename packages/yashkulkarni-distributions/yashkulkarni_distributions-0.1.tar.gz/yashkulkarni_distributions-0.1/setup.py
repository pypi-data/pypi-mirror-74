from setuptools import setup

setup(name='yashkulkarni_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['yashkulkarni_distributions'],
      zip_safe=False)
