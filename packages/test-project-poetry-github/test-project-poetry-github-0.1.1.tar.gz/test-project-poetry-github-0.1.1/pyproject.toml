[tool.poetry]
name = "test-project-poetry-github"
version = "0.1.1"
description = ""
authors = ["Victor Silva <victor.hos@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"
django = "^3.0.8"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
