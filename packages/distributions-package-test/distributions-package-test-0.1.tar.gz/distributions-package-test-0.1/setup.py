from setuptools import setup

setup(name='distributions-package-test',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions-package-test'],
      author = 'Patrick Mahomes',
      author_email = 'patmahomes90@protonmail.com',
      zip_safe=False)
