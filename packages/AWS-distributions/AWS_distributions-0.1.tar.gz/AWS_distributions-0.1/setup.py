from setuptools import setup

setup(name='AWS_distributions',
      version='0.1',
      description='Gaussian distributions and Binomial distributions',
      packages=['AWS_distributions'],
      author='Sen Yang',
      zip_safe=False)
