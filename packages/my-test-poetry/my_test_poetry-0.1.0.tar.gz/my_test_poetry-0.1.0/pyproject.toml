[tool.poetry]
name = "my_test_poetry"
version = "0.1.0"
description = "测试poetry上传到pipy"
authors = ["wangchenwei <wangchenwei@focuschina.com>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
