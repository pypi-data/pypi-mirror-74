from setuptools import setup

setup(name='GaussBinomial_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['Gauss_Binomial_distributions'],
      zip_safe=False)
