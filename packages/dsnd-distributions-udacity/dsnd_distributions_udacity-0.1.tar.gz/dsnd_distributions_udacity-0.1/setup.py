from setuptools import setup

setup(name='dsnd_distributions_udacity',
      version='0.1',
      description='dsnd_distributions udacity course',
      author = 'Alex McNulty',
      packages=['dsnd_distributions_udacity'],
      zip_safe=False)
