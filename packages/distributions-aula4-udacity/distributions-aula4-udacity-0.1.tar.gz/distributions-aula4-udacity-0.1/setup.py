from setuptools import setup

setup(name='distributions-aula4-udacity',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions-aula4-udacity'],
      zip_safe=False)
