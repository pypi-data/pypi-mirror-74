from setuptools import setup

setup(name='distributions_gn',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_gn'],
      Author='Georges Naimeh',
      zip_safe=False)
