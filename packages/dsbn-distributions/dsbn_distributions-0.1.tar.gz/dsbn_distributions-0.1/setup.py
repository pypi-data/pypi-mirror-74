from setuptools import setup

setup(name='dsbn_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['dsbn_distributions'],
      author='P. Dinagaran',
      zip_safe=False)
