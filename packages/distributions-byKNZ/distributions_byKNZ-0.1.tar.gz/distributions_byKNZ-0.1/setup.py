from setuptools import setup

setup(name='distributions_byKNZ',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions_byKNZ'],
      author="Kanza Syed",
      zip_safe=False)