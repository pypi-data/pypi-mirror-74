[tool.poetry]
name = "clipboard-util"
version = "0.1.2"
description = ""
authors = ["Eyal Levin <eyalev@gmail.com>"]
readme = "README.md"
repository = "https://github.com/eyalev/clipboard-util"

[tool.poetry.scripts]
clipboard = "clipboard_util.cli:cli"

[tool.poetry.dependencies]
python = "^3.6"
click = "^7.1.2"
pyperclip = "^1.8.0"

[tool.poetry.dev-dependencies]
pytest = "^5.2"
black = {version = "^19.10b0", allow-prereleases = true}

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
