#!/usr/bin/env python
from setuptools import setup, find_packages

setup(name='alge',
      url='',
      author='',
      package_dir={'': '.'},
      packages=find_packages('.'),
      version='0.0.1',
      install_requires=[
      ],
      include_package_data=True,
      zip_safe=True)
