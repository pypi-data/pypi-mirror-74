from setuptools import setup

setup(name='aq_udacity_distribution',
      version='0.1',
      description='Gaussian & Binomial Distributions',
      packages=['aq_udacity_distribution'],
      zip_safe=False)
