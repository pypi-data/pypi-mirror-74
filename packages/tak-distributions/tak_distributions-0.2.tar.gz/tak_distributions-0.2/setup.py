from setuptools import setup

setup(name='tak_distributions',
      version='0.2',
      description='Gaussian & Binomial distributions',
      long_description='Sample packaging for pip learning, contaning sample code for calculating Gaussian & Binomial Distribution',
      packages=['tak_distributions'],
      author = 'TAK',
      zip_safe=False)
