from setuptools import setup

setup(name='gp_gaussian_binomial_distributions',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['gp_gaussian_binomial_distributions'],
      zip_safe=False)
