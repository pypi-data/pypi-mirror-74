from setuptools import setup

setup(name='my_test_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['my_test_distributions'],
      install_requires=['matplotlib'],
      zip_safe=False)
