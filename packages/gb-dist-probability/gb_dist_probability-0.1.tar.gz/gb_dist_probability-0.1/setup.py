from setuptools import setup

setup(name='gb_dist_probability',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['gb_dist_probability'],
      zip_safe=False)
