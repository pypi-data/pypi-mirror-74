from setuptools import setup

setup(name='distributions_FHF',
      version='0.2',
      description='Gaussian distributions',
      packages=['distributions_FHF'],
      author = 'Federico Hellmund',
      zip_safe = False)