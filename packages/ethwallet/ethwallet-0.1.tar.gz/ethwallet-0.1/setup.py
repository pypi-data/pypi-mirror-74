from setuptools import setup

setup(name='ethwallet',
      version='0.01',
      description='Prints USD value of tokens and total.',
      license='MIT',
      packages=['ethwallet'],
      zip_safe=False)
