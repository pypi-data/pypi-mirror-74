from setuptools import setup

setup(name='distributions_robinlamb_test',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_robinlamb_test'],
      zip_safe=False)
